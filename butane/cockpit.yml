variant: fcos
version: 1.6.0

storage:
  files:
    - path: /etc/ssh/sshd_config.d/40-disable-passwords.conf
      mode: 0644
      overwrite: true
      contents:
        inline: |
          # Disable password logins by default.
          # https://github.com/coreos/fedora-coreos-tracker/issues/138
          # This file must sort before 50-redhat.conf, which enables
          # PasswordAuthentication.
          PasswordAuthentication yes
    - path: /etc/containers/systemd/cockpit.container
      mode: 0644
      contents:
        local: ./containers/cockpit.container
    - path: /etc/polkit-1/rules.d/50-libvirt-remote-access.rules
      mode: 0644
      contents:
        inline: |
          polkit.addRule(function(action, subject) {
            if (action.id == "org.libvirt.unix.manage" && subject.isInGroup("wheel")) {
              return polkit.Result.YES;
            }
          });

systemd:
  units:
    - name: podman-auto-update.timer
      enabled: true
    - name: cockpit.service
      enabled: false

    - name: enable-virtualization.service
      enabled: true
      contents: |
        [Unit]
        Description=Enable all virtualization sockets

        [Service]
        Type=oneshot
        ExecStart=/bin/sh -c "systemctl list-unit-files --type=socket | awk '/virt/ {print \$1}' | xargs -r systemctl enable --now"
        ExecStartPost=/usr/bin/systemctl disable enable-virtualization.service

        [Install]
        WantedBy=multi-user.target

    - name: install-cockpit.service
      enabled: true
      contents: |
        [Unit]
        Description=Install software packages via rpm-ostree
        Wants=network-online.target
        After=network-online.target
        ConditionFirstBoot=yes

        [Service]
        Type=oneshot
        ExecStartPre=/usr/bin/rpm-ostree install cockpit-system cockpit-ostree cockpit-podman libvirt qemu-kvm cockpit-machines cockpit-storaged cockpit-files cockpit-networkmanager cockpit-sosreport cockpit-kdump cockpit-selinux
        ExecStart=/bin/sh -c "systemctl enable enable-cockpit.service; systemctl disable install-cockpit.service"
        ExecStartPost=/usr/bin/rpm-ostree rebase --reboot fedora:fedora/x86_64/coreos/stable

        [Install]
        WantedBy=multi-user.target

    - name: enable-cockpit.service
      enabled: false
      contents: |
        [Unit]
        Description=Enable cockpit service
        Wants=network-online.target
        After=network-online.target

        [Service]
        Type=oneshot
        ExecStartPre=/usr/bin/systemctl enable --now cockpit.service
        ExecStart=/usr/bin/systemctl enable --now zincati.service
        ExecStartPost=/bin/sh -c "rm /var/lib/coreos-container-signing-migration/failed.stamp /etc/systemd/system/{install,enable}-cockpit.service"

        [Install]
        WantedBy=multi-user.target